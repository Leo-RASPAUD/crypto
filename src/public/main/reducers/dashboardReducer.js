/* eslint-disable indent */

const dashboardReducer = (state = {
    prices: null,
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default dashboardReducer;
